/*
type Person = {
  name: string;
};

const person: Person = {
  name: "dylan",
};

console.log(person.name);
*/

function addOne(num: number) {
  return num + 1;
}

const res = addOne(4);
console.log(res);
